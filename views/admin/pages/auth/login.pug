include ../../mixins/alert.pug
doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title
    | Signin 
    input(type='text' name='' id='')
  link( rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css")
  link(rel='stylesheet' href='/admin/css/main.css')

.main
  +alert(3000)
  form#form-1.form(action=`/${prefixAdmin}/auth/login` method='POST')
    h1.heading &Dstrok;&abreve;ng nh&#x1EAD;p
    .spacer
    .form-group
      label.form-label(for='fullName') T&ecirc;n &dstrok;&#x1EA7;y &dstrok;&#x1EE7;
      input#fullname.form-control(name='fullName' type='text' placeholder='VD: Sơn Đặng')
      span.form-message
    .form-group
      label.form-label(for='password') M&#x1EAD;t kh&#x1EA9;u
      input#password.form-control(name='password' type='password' placeholder='Nhập mật khẩu')
      span.form-message
    button.form-submit &Dstrok;&abreve;ng nh&#x1EAD;p
  script( src = "/admin/js/login.js")
